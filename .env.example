# Copy this file to .env and fill in your values
# .env is gitignored — never commit it
# .env.example is committed — it shows other devs what variables are needed

DB_HOST=localhost
DB_PORT=5432
DB_NAME=employeedb
DB_USER=appuser
DB_PASSWORD=secret123
