apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
type: Opaque
stringData:
  POSTGRES_PASSWORD: <YOUR_DB_PASSWORD>
  # Replace <YOUR_DB_PASSWORD> with your actual password before applying.
  # In production, use GCP Secret Manager + External Secrets Operator
  # instead of storing credentials in YAML files.
